pipeline {
    agent any
    // Set the environment variable APP_PORT=9090
    stages {
        stage('Build') {
            steps {
            }
        }
        stage('Unit Test') {
            steps {
            }
        }
    }
}
